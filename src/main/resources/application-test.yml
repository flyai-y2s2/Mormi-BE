spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

mormi:
  cors:
    allowed-origins: http://localhost:3000
  dialogue:
    read-timeout-seconds: 45
    base-url: ""
    service-key: ""
