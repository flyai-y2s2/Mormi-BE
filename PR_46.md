## #️⃣ 연관된 이슈

- Closes #46
- 함께 확인할 AI 이슈: [flyai-y2s2/Mormi-AI#39](https://github.com/flyai-y2s2/Mormi-AI/issues/39)

## #️⃣ 작업 내용

- 놀이동산 카탈로그의 화면용 존댓말 지시문과 AI L4 첫 턴용 모르미 대사를 분리했습니다.
- AI 첫 턴 대사를 방문에 고정된 실제 숫자로 생성하도록 변경했습니다.
  - 매표소: `표 한 장이 3,000원이고 2명이 가면 모두 얼마야?`
  - 간식가게: `6,000원인 간식을 3명이 똑같이 내려면 한 명은 얼마씩 내야 해?`
  - 자유이용권 창구: `한 번에 2,000원이고 자유이용권은 10,000원이야. 몇 번부터 자유이용권이 더 이득일까?`
- FE 완료 화면에서 사용하는 `multiply` / `divide` / `compare` enum은 유지했습니다.
- AI의 `visible_facts`에 들어가는 `park_context.skill`은 별도의 한국어 개념 라벨로 전달하도록 분리했습니다.
  - `같은 값 여러 번 더하기`
  - `똑같이 나누기`
  - `값을 나누고 비교하기`
- 화면 API가 기존 `skill` enum과 존댓말 문제 지시문을 그대로 반환하는지 회귀 검증을 추가했습니다.
- `ParkDialogueAiContractTest`에 첫 턴의 반말 질문 어투, 방문 숫자 반영, 화면 문구와의 분리, 한국어 skill 전달 검증을 추가했습니다.

## #️⃣ 테스트 결과

- Java 21로 변경된 운영 코드와 테스트 클래스 컴파일 성공
- 놀이동산 관련 계약 테스트 13개 통과
  - `AmusementParkCatalogTest`: 10개 통과
  - `ParkDialogueAiContractTest`: 3개 통과
- 인접한 실제 Mormi-AI 체크아웃을 사용한 `SessionCreate` 스키마 검증 통과
- `AmusementParkFlowIntegrationTest` 컴파일 성공
- 현재 작업 환경에서 Gradle의 파일 잠금 조정용 로컬 소켓 생성이 차단되어 `./gradlew test` 전체 실행은 수행하지 못했습니다. 전체 테스트는 PR CI의 `./gradlew --no-daemon clean build`에서 확인이 필요합니다.

## #️⃣ 변경 사항 체크리스트

- [x] 코드에 영향이 있는 모든 부분에 대한 테스트를 작성하고 실행했나요?
- [x] 문서를 작성하거나 수정했나요? (외부 API 필드 변경은 없어 별도 API 문서 수정은 불필요합니다.)
- [x] 코드 컨벤션에 따라 코드를 작성했나요?
- [x] 본 PR에서 발생할 수 있는 모든 의존성 문제가 해결되었나요? (신규 의존성 없음)

## #️⃣ 스크린샷 (선택)

- BE의 AI 요청 컨텍스트 변경으로 화면 변경 사항은 없습니다.

## #️⃣ 리뷰 요구사항 (선택)

- FE 방문 응답에는 기존 `skill` enum과 화면용 `prompt`가 유지되고, AI 요청의 `park_context`에서만 한국어 `skill`과 모르미 반말 `prompt`가 사용되는지 확인 부탁드립니다.
- Mormi-AI#39 변경과 함께 적용했을 때 L4 첫 턴 및 이후 speaker 컨텍스트의 어투가 일관적인지 확인 부탁드립니다.
- 자유이용권 첫 질문이 `break_even_rides`와 `benefit_from_rides`를 모두 수집하는 기존 AI 단계 흐름과 자연스럽게 이어지는지 확인 부탁드립니다.

## 📎 참고 자료 (선택)

- #46 이슈 설명 및 재현 경로
- [flyai-y2s2/Mormi-AI#39](https://github.com/flyai-y2s2/Mormi-AI/issues/39)
